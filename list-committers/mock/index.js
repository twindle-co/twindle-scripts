const { response } = require("./data");

console.log(response[0]);

// Try uncommenting the following line to see some data manipulation
// console.log(response.map((commit) => commit.author.login));
